# Terraform state — contains secrets, never commit
*.tfstate
*.tfstate.backup

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Plan files
tfplan
*.tfplan

# Var files with secrets (keep terraform.tfvars only if it has no real secrets)
*.auto.tfvars

# OS
.DS_Store
Thumbs.db
